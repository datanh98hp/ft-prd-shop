import { TimezoneMiddleware } from './timezone-middleware.middleware';

describe('TimezoneMiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new TimezoneMiddleware()).toBeDefined();
  });
});
